name: Digital Organism Health Check

on:
  schedule:
    # Kiểm tra sức khỏe mỗi 12 giờ
    - cron: '0 */12 * * *'
  workflow_dispatch:
    inputs:
      generate_report:
        description: 'Generate detailed health report'
        required: false
        type: boolean
        default: true

permissions:
  contents: write
  issues: write

jobs:
  health-check:
    runs-on: ubuntu-latest
    steps:
      - name: 🧬 Checkout Digital Organism
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🐍 Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: 📦 Install Dependencies
        run: |
          pip install PyGithub pyyaml

      - name: 🏥 Run Health Monitor
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO_NAME: ${{ github.repository }}
        run: |
          chmod +x .github/scripts/health_monitor.py
          chmod +x .github/scripts/ecosystem_health_diagnostic.py
          
          echo "🔍 Running comprehensive health diagnostic..."
          python .github/scripts/ecosystem_health_diagnostic.py || true
          
          echo "🏥 Running GitHub health monitor..."
          python .github/scripts/health_monitor.py

      - name: 💾 Commit Health Report
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "Digital Organism 🧬"
          
          if [[ -n $(git status -s metrics/) ]]; then
            git add metrics/
            TIMESTAMP=$(date -u +"%Y-%m-%d %H:%M UTC")
            git commit -m "🏥 Health check: $TIMESTAMP - Auto-generated health report"
            git push
          else
            echo "No new health data to commit"
          fi

      - name: 📊 Health Summary
        if: always()
        run: |
          echo "## 🧬 Digital Organism Health Check" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          # Ecosystem diagnostic results
          if [ -f metrics/ecosystem_health_diagnostic_*.json ]; then
            LATEST_DIAGNOSTIC=$(ls -t metrics/ecosystem_health_diagnostic_*.json | head -1)
            DIAGNOSTIC_HEALTH=$(python -c "import json; print(json.load(open('$LATEST_DIAGNOSTIC'))['health_score'])")
            TOTAL_ISSUES=$(python -c "import json; print(json.load(open('$LATEST_DIAGNOSTIC'))['total_issues'])")
            
            echo "**Ecosystem Health**: $DIAGNOSTIC_HEALTH%" >> $GITHUB_STEP_SUMMARY
            echo "**Total Issues**: $TOTAL_ISSUES" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
          fi
          
          # GitHub API health results
          if [ -f metrics/health_report_*.json ]; then
            LATEST_REPORT=$(ls -t metrics/health_report_*.json | head -1)
            HEALTH=$(python -c "import json; print(json.load(open('$LATEST_REPORT'))['overall_health'])")
            
            echo "**GitHub API Health**: $HEALTH" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "✅ Health monitoring completed" >> $GITHUB_STEP_SUMMARY
          else
            echo "⚠️ No health report generated" >> $GITHUB_STEP_SUMMARY
          fi
