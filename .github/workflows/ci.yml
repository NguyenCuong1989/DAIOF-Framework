name: DAIOF CI/CD

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

# Principle of Least Privilege: only read access for CI checks
permissions:
  contents: read

jobs:
  test:
    name: Test on Python ${{ matrix.python-version }}
    runs-on: ${{ matrix.os }}
    
    strategy:
      matrix:
        # Reduced to ubuntu-only to cut CI cost; framework is pure Python with no
        # platform-specific code.  Re-add macos-latest/windows-latest if native
        # extensions are introduced.
        os: [ubuntu-latest]
        python-version: ['3.9', '3.10', '3.11', '3.12']
    
    steps:
    - name: 📥 Checkout code
      env:
        GH_TOKEN: ${{ github.token }}
      run: |
        git init -q .
        git remote add origin "https://x-access-token:${GH_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
        git fetch -q --depth 1 origin "${GITHUB_REF}"
        git checkout -q FETCH_HEAD
    - name: 🐍 Set up Python ${{ matrix.python-version }}
      run: python3 --version
    - name: 📦 Install dependencies
      run: |
        python3 -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pytest pytest-cov flake8
    
    - name: 🔍 Lint with flake8 (critical errors only)
      run: |
        flake8 digital_ai_organism_framework.py haios_core.py haios_runtime.py scripts tools tests --count --select=E9,F63,F7,F82 --show-source --statistics
    
    - name: 🧬 Verify immutable genes
      run: |
        python3 -c "
        from digital_ai_organism_framework import DigitalGenome
        genome = DigitalGenome()
        assert 'human_dependency_coefficient' in genome.traits
        assert genome.traits['human_dependency_coefficient'] == 1.0
        print('✅ Immutable genes verified')
        "

    - name: 🏛️ Verify doctrine compliance
      run: |
        python3 tests/test_doctrine_compliance.py
        echo '✅ Doctrine compliance verified'

    - name: 🧪 Run tests
      run: |
        PYTHONPATH="$PWD" pytest tests/ -v --cov=digital_ai_organism_framework --cov-report=xml --cov-report=term
    
  validate:
    name: Validate Framework
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout code
      env:
        GH_TOKEN: ${{ github.token }}
      run: |
        git init -q .
        git remote add origin "https://x-access-token:${GH_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
        git fetch -q --depth 1 origin "${GITHUB_REF}"
        git checkout -q FETCH_HEAD
    - name: 🐍 Set up Python
      run: python3 --version
    - name: 📦 Install dependencies
      run: |
        python3 -m pip install --upgrade pip
        pip install -r requirements.txt
    
    - name: 🎼 Test Symphony Control Center
      run: |
        python3 -c "
        from digital_ai_organism_framework import SymphonyControlCenter
        symphony = SymphonyControlCenter()
        print('✅ Symphony Control Center functional')
        "
    
    - name: 🧬 Test Digital Organism lifecycle
      run: |
        python3 -c "
        from digital_ai_organism_framework import DigitalOrganism
        organism = DigitalOrganism('ci-lifecycle')
        organism.live_cycle(0.1)
        assert organism.status == 'alive'
        assert organism.health > 0
        assert organism.metabolism.get_resource_health() > 0
        print(f'✅ Organism lifecycle working (health: {organism.health:.2f})')
        "
    
    - name: 🌍 Test Ecosystem
      run: |
        python3 -c "
        from digital_ai_organism_framework import DigitalEcosystem, DigitalOrganism
        ecosystem = DigitalEcosystem('ci-ecosystem')
        for i in range(5):
            ecosystem.add_organism(DigitalOrganism(f'ci-ecosystem-{i}'))
        ecosystem.simulate_time_step(0.1)
        print(f'✅ Ecosystem functional with {len(ecosystem.organisms)} organisms')
        "

  security:
    name: Security Scan
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout code
      env:
        GH_TOKEN: ${{ github.token }}
      run: |
        git init -q .
        git remote add origin "https://x-access-token:${GH_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
        git fetch -q --depth 1 origin "${GITHUB_REF}"
        git checkout -q FETCH_HEAD
    - name: 🐍 Set up Python
      run: python3 --version
    - name: 🔐 Run bandit security linter
      run: |
        pip install bandit
        bandit -r digital_ai_organism_framework.py haios_core.py haios_runtime.py src/ -f json -o bandit-report.json || true
        bandit -r digital_ai_organism_framework.py haios_core.py haios_runtime.py src/ -ll
      continue-on-error: true

  release:
    name: Create Release
    runs-on: ubuntu-latest
    needs: [test, validate]
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write
    
    steps:
    - name: 📥 Checkout code
      env:
        GH_TOKEN: ${{ github.token }}
      run: |
        git init -q .
        git remote add origin "https://x-access-token:${GH_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
        git fetch -q --depth 1 origin "${GITHUB_REF}"
        git checkout -q FETCH_HEAD
    - name: 🎉 Create Release (gh CLI, no external action)
      env:
        GH_TOKEN: ${{ github.token }}
      run: |
        gh release create "${GITHUB_REF_NAME}" \
          --title "DAIOF ${GITHUB_REF_NAME}" \
          --notes "## Digital AI Organism Framework Release

        See [CHANGELOG.md](CHANGELOG.md) for details.

        **Status**: Production Ready

        Creator: Alpha_Prime_Omega (4287)
        Maintainer: Digital AI Organism"
