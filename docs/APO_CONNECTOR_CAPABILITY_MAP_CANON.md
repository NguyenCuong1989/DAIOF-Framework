# CONNECTOR CAPABILITY MAP → APΩ AGENT NETWORK — CANON

**Root Law: PHẢI SỐNG**

**Protocol: D&R — Deconstruct → Focal Point → Re-architecture**

## 1. Connector substrate

`C = {K,D,C,R,Dsg,M,Com,F,Res,Sec,Dep}`

`Connector = Interface + Capability + Authority + Boundary + Evidence`

The registry is a capability substrate, not a list of independent applications.

## 2. Four-phase architecture

### HÔ PHONG HOÁN VŨ
`Intent → Capability Discovery → Routing → Network Execution`

### RẢI ĐẬU THÀNH BINH
`Capability → Seed → Agent → Tool Binding → Cross-Agent Linkage → Network`

### TRỜI LONG ĐẤT LỞ
`Network → Large-Scale State/Topology Transformation`

### TRĂNG KHUYẾT LẠI TRÒN
`State_t → Change → Evidence → Verification → Reconstitution → State_{t+1}`

## 3. Network

`N=(V,E,C,P,A)` where V=agents, E=typed handoffs, C=capability bindings, P=policy, A=authority.

Cross-domain authority is never implicit. Cross-domain execution requires orchestration through the admissibility boundary.

## 4. Cấm Chế

`Forbidden = StateSpace \\ Admissible`

`S_t ∈ Forbidden OR u_t ∈ ForbiddenU ⇒ S_stop`

Only PASS continues. REJECT, STOP and CLOSE terminate the relevant transition.

## 5. Continuity

Preserve `Canon + Invariant + Identity + Causal Lineage`. Continuity is causal, not snapshot identity; `State_{t+1}` may differ from `State_t`.

## 6. Agent seed mapping

K→KnowledgeAgent; D→DatabaseAgent; C→CodeAgent; R→ExecutionAgent; Dsg→DesignAgent; M→MediaAgent; Com→CoordinationAgent; F→FinanceAgent; Res→ResearchAgent; Sec→SecurityAgent; Dep→DeploymentAgent.

## 7. Hard invariants

- Minimum authority / no over-privilege.
- No implicit cross-domain mutation.
- Spawn only within CPU, memory, IO and CloneTTL budgets.
- No invalid topology traversal.
- No Forbidden state/control transition.
- No continuity claim without causal evidence.
- Fail closed on boundary, authority, resource, invariant or evidence failure.
- Capability scale requires governance scale.

## 8. Canonical transformation

`Connectors (Capability Seeds) → Agents (Spawned Forces) → Network (Linked Intelligence) → Transformation (Systemic Power) → Reconstitution (Continuity)`
