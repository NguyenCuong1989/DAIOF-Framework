# APΩ — WC-DM DRIFT PREVENTION LAW

> **FLOW NODE 2/12** — entered from [`00_TODO_RESUME.md`](00_TODO_RESUME.md); exits to [`MANIFEST.md`](MANIFEST.md). This is the first mandatory law gate.

**Status: HARD / CANONICAL**

## L0 — NO DRIFT
A research-direction change is admissible only when forced by canonical ontology, an explicitly recorded GAP, a source-audit result, or falsification.

`NO_NEW_GAP → NO_NEW_BRANCH`
`NO_NEW_EVIDENCE → NO_NEW_CONSTRAINT`
`NO_NEW_VALIDATED_RELATION → NO_NEW_COMPUTATION`

## L1 — SINGLE STATE AUTHORITY
`00_TODO_RESUME.md` is the operational resume authority. `MANIFEST.md` is the integrity/state manifest. Historical execution records are not state authority.

Resume sequence:
`LOAD → LAW GATE → VERIFY CANON → VERIFY OPEN GAPS → OPEN FIRST GAP → ACT`

Stale or contradictory artifacts MUST NOT override verified canonical state.

## L2 — PHYSICAL STATE LOCK
Unless a formally admitted constraint changes it:

`G_full=(0.3928,0.4107)`
`G_cons=(0.3870,0.4167)`
`SURVIVE — g_dark NOT CONCLUDED`

These intervals are state objects, never target inputs. `x=0.4`, `x=0.4018`, and `x=0.4019` MUST NOT enter target fitting, model selection, parameter choice, search selection, or initialization as desired values.

## L3 — GAP-FIRST LOCK
Every action MUST identify exactly one primary open GAP:

`GAP → REQUIRED OBJECT → SOURCE → PROVENANCE → COUPLING → ADMISSION → COMPUTATION → FALSIFICATION → DEPENDENCY → INTERSECTION → VERIFY → SAVE`

If no GAP is open, rescan ontology. Do not invent a new branch merely to continue activity.

## L4 — FIRST-OPEN-GAP PRIORITY
The first unresolved prerequisite on the dependency path has priority over attractive later domains.

Current physical resume point:
`G3 — FULL K_z(R,z) SOURCE / RELATION / PROVENANCE AUDIT`

G6 is resolved as `COUPLING_GAP / MODEL_IDENTIFICATION_GAP` and MUST NOT be reopened without genuinely new source evidence addressing the missing coupling.

## L5 — READ-ONLY BEFORE MUTATION
Before modifying canonical state, data, mappings, or computational artifacts:
`SCAN → DEPENDENCY MAP → ROOT CAUSE → IMPACT BOUNDARY → SIMULATE/VERIFY → MUTATE`

No mutation is permitted merely because a result is inconvenient or inconsistent with a survivor.

## L6 — TYPE LOCK
`E ≠ I ≠ M ≠ A ≠ C ≠ G`

Forbidden implicit promotion:
`MODEL → EVIDENCE`
`COMPUTATION → EVIDENCE`
`INFERENCE → MEASUREMENT`
`ROBUSTNESS → INDEPENDENT_EVIDENCE`

## L7 — COUPLING LOCK
`¬CouplingValidated → ¬Computation → ¬Constraint`

A related literature limit is not a coupling to `x` unless the forward relation is source-locked, dimensionally valid, domain-valid, non-circular, and parameter-provenanced.

## L8 — EVIDENCE LOCK
Every numerical datum entering physical computation MUST have observable, measurement, units, uncertainty, provenance, and lineage. Missing evidence means `QUARANTINE`, not substitution.

## L9 — DEPENDENCY LOCK
Unknown dependency MUST NOT be treated as independence. Inspect sample, observable, systematic, processing, calibration, model, and source. Material correlation requires covariance-aware or conservative combination.

## L10 — FALSIFICATION LOCK
Every admitted constraint MUST state what region it excludes and how exclusion is tested.

`G_next = ∅ → FALSIFIED / CHẾT`

An unresolved coupling/data/model gap is not falsification. Do not silently alter model/data/mapping to restore survival.

## L11 — SEARCH LOCK
Search has discovery authority only:
`SEARCH → CANDIDATE`

Never `SEARCH → EVIDENCE` or `SEARCH → CONSTRAINT`.
Search MUST NOT be selected by desired numerical outcome.

## L12 — EXECUTION / EVIDENCE SEPARATION
Code output, simulation, generated tables, and execution logs are `EXECUTION_RECORDS` until evidence, provenance, equations, coupling, and validation are audited. Repetition does not increase evidentiary authority.

## L13 — CHECKPOINT LAW
Every completed action MUST update `00_TODO_RESUME.md`, the relevant lineage/audit artifact, and `MANIFEST.md` when state/law hierarchy changes, then commit. Work existing only in conversation is not canonical.

## L14 — LAW-CHANGE LOCK
A LAW may not be weakened, bypassed, or silently reinterpreted to admit a blocked result. Apparent conflicts require:
`HALT → precedence audit → explicit resolution → resume`

## L15 — NO RETROACTIVE CLEANUP
Historical records MUST NOT be rewritten merely to make lineage cleaner. Corrections append a correction/audit record and preserve prior state.

## L16 — COMPLETION CLAIM LOCK
`SPECIFIED ≠ IMPLEMENTED ≠ VERIFIED ≠ CLOSED`.

A weakness is CLOSED only when its required artifact/test/audit exists and the result is recorded.

## L17 — MASTER DRIFT INVARIANT
For every transition `S_t → S_{t+1}`:

`Allowed = LawPass ∧ GapJustified ∧ TypePreserved ∧ ProvenancePreserved ∧ NoTargetInjection ∧ DependencyHandled ∧ FalsificationHandled`

If any term is **false OR unknown**: `HALT`.

## L18 — UNIVERSAL RESUME COMMAND
Every future session MUST begin:

`00_TODO_RESUME.md → LAW-DRIFT-PREVENTION.md → MANIFEST.md → FIRST OPEN GAP`

Current:
`FIRST OPEN GAP = G3`
`NEXT ACTION = READ-ONLY G3 AUDIT`
`NO PHYSICAL COMPUTATION UNTIL G3 COUPLING PASSES`

## MASTER LAW

**DO NOT ASK WHAT CAN BE DONE NEXT. ASK WHAT THE CURRENT CANONICAL STATE LEGALLY PERMITS NEXT.**

**NEXT NODE:** [`MANIFEST.md`](MANIFEST.md).