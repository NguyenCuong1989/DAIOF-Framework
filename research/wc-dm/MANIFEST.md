# APΩ — WC-DM PRODUCTION MANIFEST

> **DOCUMENT GRAPH / CLOSED-LOOP AUTHORITY.** Entered from [`00_TODO_RESUME.md`](00_TODO_RESUME.md); exits to [`01_canonical_checkpoint.md`](01_canonical_checkpoint.md). It defines authority and graph topology and never overrides LAW or canonical physical state.

## 0. CLOSED LOOP

`00_TODO_RESUME → LAW GATES → CANONICAL STATE → ONTOLOGY → EVIDENCE → AXCONTROL → ROOT-CAUSE → ACTIVE GAP → EXECUTION/AUDIT → CHECKPOINT → NEXT TODO → COMMIT → 00_TODO_RESUME`

Every completed cycle must return to `00_TODO_RESUME.md` with a newly generated actionable TODO. A checkpoint without a next TODO is **NOT CLOSED**.

## 1. AUTHORITY HIERARCHY

`LAW_* / LAW-*` = normative governance.  
`MANIFEST` = authority + document graph.  
`01_CHECKPOINT` = canonical physical state + lineage.  
`00_TODO_RESUME` = operational resume pointer + current TODO.  
`02–04` = ontology/evidence/discovery methodology.  
`ACTIVE AUDIT` = current GAP investigation.  
`05–10` = execution/historical records.

**Conflict:** `HALT → resolve authority → resume.`

## 2. LOCKED STATE

`x = g_dark / g_crit`  
`G_full=(0.3928,0.4107) g_crit`  
`G_cons=(0.3870,0.4167) g_crit`  
`g_dark = NOT CONCLUDED`

Target values are outputs only; target injection is forbidden.

## 3. CURRENT RESUME

`FIRST_OPEN_GAP = G3`

G3 requires a full `K_z(R,z)` source/provenance/units/sign/normalization/validated-relation/WC-DM-coupling audit.

G6 = `COUPLING_GAP / MODEL_IDENTIFICATION_GAP`; it is not a numerical constraint and may reopen only with genuinely new relevant source evidence.

## 4. HARD INVARIANTS

`NO_NEW_GAP → NO_NEW_BRANCH`  
`NO_NEW_EVIDENCE → NO_NEW_CONSTRAINT`  
`NO_NEW_VALIDATED_RELATION → NO_NEW_COMPUTATION`  
`UNKNOWN → HALT`  
`¬CouplingValidated → ¬Computation → ¬Constraint`  
`NO_CHECKPOINT_WITHOUT_NEXT_TODO`  
`NO_CYCLE_CLOSED_WITHOUT_COMMIT`  
`NEXT_TODO_MUST_BE_DERIVED_FROM_OPEN_GAPS`

## 5. DOCUMENT GRAPH

| Node | Role | Direction |
|---|---|---|
| `00_TODO_RESUME.md` | operational start + current TODO | → LAW |
| `LAW-DRIFT-PREVENTION.md` | anti-drift gate | → MANIFEST |
| `MANIFEST.md` | authority + graph | → CHECKPOINT |
| `01_canonical_checkpoint.md` | canonical state | → ONTOLOGY |
| `02_domain_object_ontology.md` | object typing | → EVIDENCE |
| `03_evidence_gate_and_rules.md` | evidence admission | → LLM LAW |
| `LAW-LLM-EPISTEMIC-CONTAINMENT.md` | epistemic containment | → AXCONTROL |
| `LAW-AXCONTROL-CODEGEN-CONTRACT.md` | artifact integrity | → ROOT-CAUSE LAW |
| `LAW_UNIVERSAL_ROOT_CAUSE_INFERENCE.md` | inference governance | → SEARCH |
| `04_domain_search_matrix.md` | discovery | → ACTIVE GAP |
| `05_execution_record.md` | execution boundary | → CHECKPOINT |
| `07_domain_candidate_register.md` | candidate subgraph | → SEARCH / RESUME |
| `10_G6_external_source_coupling_audit.md` | closed G6 audit | → CHECKPOINT / G3 |
| `06_*`, `08_*` | historical discovery | → lineage only |

## 6. PRODUCTION CYCLE CONTRACT

A cycle is `CLOSED` only when:

1. the current GAP has a dedicated artifact;
2. source/provenance status is recorded;
3. coupling status is explicit;
4. computation/constraint status is explicit;
5. falsification/dependency status is explicit when applicable;
6. canonical checkpoint is updated;
7. `00_TODO_RESUME.md` contains the next actionable TODO;
8. all changes are committed;
9. the commit is the new resume anchor.

`SPECIFIED ≠ IMPLEMENTED ≠ VERIFIED ≠ CLOSED`.

## 7. FAIL-CLOSED RESUME

A fresh session needs only the repository link and the canonical entrypoint. It must read the flow once, identify the current TODO, execute only the authorized gap, close the cycle, generate the next TODO, commit, and return to the entrypoint.

If the graph, state, TODO, or authority chain disagree: **HALT.**

If `OPEN_GAPS = ∅`: rescan ontology. Do not invent a domain merely to continue.

**NEXT NODE:** [`01_canonical_checkpoint.md`](01_canonical_checkpoint.md)  
**RETURN NODE:** [`00_TODO_RESUME.md`](00_TODO_RESUME.md)